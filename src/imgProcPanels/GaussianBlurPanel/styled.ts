import styled from "@emotion/styled";
import { FormControl } from "@mui/material";

export const StyledSubMethodForm = styled(FormControl)`
  display: flex;
  flex-direction: column;
  gap: 15px;
`;

export const StyledInputsRowWrapper = styled("div")`
  display: flex;
  gap: 10px;
`;
