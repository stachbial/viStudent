import ThresholdOperation from "../ThresholdOperation";
import Divider from "../../components/Divider";

// TODO: ogarnac formsa + number inputs validation i button "wykonaj progowanie proste"
// TODO: ogarnac progowanie adapcyjne

const NormalizationPanel = () => {
  return (
    <>
      <ThresholdOperation />
      <Divider />
    </>
  );
};

export default NormalizationPanel;
